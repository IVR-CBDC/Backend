# service-core: домен сделки — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Превратить service-core из заглушки (`compute`, `status`) в домен сделки: сделки, документы, таймлайн, уведомления, авторитетный пересчёт комиссии, доставка событий через outbox → Redis и детерминированный эмулятор внешних проверок.

**Architecture:** Вся доменная логика переходов вынесена в чистый `DealStateMachine` (без Drogon и БД) и покрыта Catch2. Слой `DealRepository` применяет результат перехода в одной транзакции вместе с записью в `outbox`; отдельный цикл публикует outbox в Redis `deal-events:{company_id}`, откуда BFF раздаёт события в WebSocket. Эмулятор внешних систем живёт внутри core, ходит по `next_action_at` через `FOR UPDATE SKIP LOCKED` и детерминирован по `deal_id` (FNV-1a), поэтому демо воспроизводимо. Изоляция арендатора — `company_id` из JWT: она проверяется не юнит-тестами, а pytest-тестами против поднятого стенда, которые этот план заводит.

**Tech Stack:** C++20, Drogon 1.9.11 (корутины, `getRedisClient`, `HttpClient`), libjwt, PostgreSQL 16, Redis 7, Catch2 v3.7.1, pytest + httpx (API-тесты), Docker Compose.

**Spec:** `docs/superpowers/specs/2026-09-15-cbdc-hub-integration-design.md` (§4 целиком, §3 роль core, §9 строки «Unit C++» и «API»)

## Global Constraints

- Все команды — из корня репо `/home/legors/Documents/IVR`, ветка `feat/core-deals` (создаётся в Task 1), базовая ветка `main`.
- Конверт ошибок: `{"code": "<UPPER_SNAKE>", "error": "<сообщение по-русски>"}` через `common::jsonError(status, code, message)`. Коды core: `INVALID_JSON`, `VALIDATION_ERROR`, `NOT_FOUND`, `VERSION_CONFLICT`, `INVALID_TRANSITION`, `SCENARIO_UNAVAILABLE`, `UPSTREAM_UNAVAILABLE`, `INTERNAL_ERROR`, плюс `UNAUTHORIZED`/`INVALID_TOKEN` от `common::JwtFilter`.
- Все доменные ручки — под `"common::JwtFilter"`; `user_id` и `company_id` берутся из `req->attributes()`. Чужая сделка отдаёт `404 NOT_FOUND`, а не `403` (не раскрываем существование).
- Все SQL-запросы параметризованные (`$1`, `$2`, …). Никаких конкатенаций.
- Деньги: `NUMERIC(18,2)` в БД, в JSON — числа. Стадии, статусы документов и статусы шагов — ровно те значения, что перечислены в спеке §4.1.
- Порядок сценариев везде: `cbdc`, `bank_transfer`, `smart_contract`, `trade_finance`.
- Домашняя страна клиента — `RU`: для `import` `from=RU, to=counterparty`, для `export` `from=counterparty, to=RU` (как в плане 01).
- `display_id` = `DEAL-<год created_at>-<display_no, минимум 4 цифры с ведущими нулями>`.
- Тесты C++ собираются только при `-DBUILD_TESTS=ON`; Docker-сборки сервисов тесты не собирают. `make test-cpp` должен оставаться зелёным после каждого коммита.
- Никаких операций с кластером и `git push`. Ветка локальная. Контейнеры `registry`, `legors-db`, `tealdeer-db` не трогать.
- Порт 8081 на машине занят посторонним процессом, поэтому traefik может не подняться: проверять сервисы напрямую в сети `ivr_backend-net` (`docker compose exec`, `docker run --rm --network ivr_backend-net curlimages/curl`).
- Сообщение коммита заканчивается строкой `Co-Authored-By: Claude <модель> <noreply@anthropic.com>` — модель та, что реально пишет коммит.

## File Structure

```
services/service-core/migrations/002_deals.sql        NEW  весь домен + seed document_kinds, удаление _placeholder
services/service-core/include/
  domain.h                 NEW  типы домена (Deal, Document, TimelineStep, enum-строки)
  state_machine.h          NEW  DealStateMachine — чистые переходы
  repository.h             NEW  DealRepository — БД + outbox в одной транзакции
  commission_client.h      NEW  HTTP-клиент к service-commission
  emulator.h               NEW  детерминированный эмулятор внешних систем
  outbox_publisher.h       NEW  цикл outbox → Redis
  core_controller.h        MOD  ручки домена вместо compute/status
services/service-core/src/
  domain.cc                NEW  парсинг/сериализация значений домена
  state_machine.cc         NEW
  repository.cc            NEW
  commission_client.cc     NEW
  emulator.cc              NEW
  outbox_publisher.cc      NEW
  main.cc                  MOD  запуск publisher и эмулятора после старта фреймворка
  core/health.cc           MOD  + redis_ok
  core/deals.cc            NEW  GET/POST /api/core/deals, GET /api/core/deals/{id}
  core/scenario.cc         NEW  POST /api/core/deals/{id}/scenario
  core/documents.cc        NEW  POST /api/core/deals/{id}/documents/{docId}/submit
  core/notifications.cc    NEW  GET /api/core/notifications, POST /{id}/read
  core/emulator_tick.cc    NEW  POST /internal/emulator/tick (только при EMULATOR_MANUAL=true)
  core/compute.cc          DEL
  core/status.cc           DEL
services/service-core/tests/                          NEW  CMakeLists.txt, test_state_machine.cc, test_domain.cc, test_emulator.cc
services/service-core/config.json.tpl                 MOD  + redis_clients
tests/api/                                            NEW  conftest.py, test_core_deals.py, pyproject.toml
docker-compose.yml                                    MOD  pg-core наружу 127.0.0.1:5435, env core (REDIS_*, EMULATOR_*, COMMISSION_URL)
Makefile                                              MOD  test-api, smoke-цели домена
docs/openapi.yml, README.md                           MOD  перегенерация и описание домена
```

---

### Task 1: Схема домена и миграции

**Files:**
- Create: `services/service-core/migrations/002_deals.sql`
- Modify: `docker-compose.yml`

**Interfaces:**
- Consumes: существующий `infra/migrate.sh` (нумерованные `.sql`, таблица `schema_migrations`).
- Produces: схема `pg-core` (таблицы `document_kinds`, `deals`, `deal_documents`, `timeline_events`, `notifications`, `outbox`), `pg-core` на `127.0.0.1:5435`.

- [ ] **Step 1: Создать ветку**

Run: `git checkout main && git status --short && git checkout -b feat/core-deals`
Expected: дерево чистое, ветка создана.

- [ ] **Step 2: Написать `services/service-core/migrations/002_deals.sql`**

```sql
-- Домен сделки. Всё разделено по company_id: он приезжает в JWT (claim company_id)
-- и является единственной границей между арендаторами.
DROP TABLE IF EXISTS _placeholder;

-- Справочник документов: какие нужны и зачем (текст показывается пользователю).
CREATE TABLE document_kinds (
    kind         TEXT PRIMARY KEY,
    title        TEXT   NOT NULL,
    purpose      TEXT   NOT NULL,
    required_for TEXT[] NOT NULL
);

INSERT INTO document_kinds (kind, title, purpose, required_for) VALUES
    ('contract',   'Контракт',
     'Основание для валютного контроля: банк сверяет условия сделки с договором.',
     ARRAY['cbdc', 'bank_transfer', 'smart_contract', 'trade_finance']),
    ('invoice',    'Инвойс',
     'Подтверждает сумму и состав поставки.',
     ARRAY['cbdc', 'bank_transfer', 'smart_contract', 'trade_finance']),
    ('deal_passport', 'Паспорт сделки (УНК)',
     'Уникальный номер контракта: нужен банку для постановки сделки на учёт.',
     ARRAY['bank_transfer', 'trade_finance']),
    ('letter_of_credit', 'Заявление на аккредитив',
     'Поручение банку раскрыть аккредитив в пользу контрагента.',
     ARRAY['trade_finance']),
    ('digital_signature', 'Подтверждение ЭЦП сторон',
     'Смарт-контракт исполняется только при подписи обеих сторон.',
     ARRAY['smart_contract']);

CREATE TABLE deals (
    id                   UUID PRIMARY KEY,
    company_id           UUID          NOT NULL,
    display_no           BIGINT        GENERATED ALWAYS AS IDENTITY,
    counterparty_country CHAR(2)       NOT NULL,
    counterparty_name    TEXT          NOT NULL CHECK (length(trim(counterparty_name)) > 0),
    operation_type       TEXT          NOT NULL CHECK (operation_type IN ('import', 'export')),
    amount               NUMERIC(18,2) NOT NULL CHECK (amount > 0),
    currency             CHAR(3)       NOT NULL,
    scenario             TEXT          CHECK (scenario IN ('cbdc', 'bank_transfer', 'smart_contract', 'trade_finance')),
    commission_total     NUMERIC(18,2),
    commission_breakdown JSONB,
    stage                TEXT          NOT NULL
                         CHECK (stage IN ('created', 'documents', 'compliance_check', 'settlement', 'completed', 'blocked')),
    blocked_from         TEXT          CHECK (blocked_from IN ('documents', 'compliance_check', 'settlement')),
    blocker_reason       TEXT,
    next_action_at       TIMESTAMPTZ,  -- когда эмулятору трогать сделку; NULL = ждём пользователя
    version              INT           NOT NULL DEFAULT 0,
    created_at           TIMESTAMPTZ   NOT NULL DEFAULT now(),
    updated_at           TIMESTAMPTZ   NOT NULL DEFAULT now(),
    CHECK ((stage = 'blocked') = (blocked_from IS NOT NULL))
);

CREATE INDEX deals_company_idx ON deals(company_id, updated_at DESC);
CREATE INDEX deals_next_action_idx ON deals(next_action_at)
    WHERE next_action_at IS NOT NULL AND stage <> 'completed';

CREATE TABLE deal_documents (
    id             UUID PRIMARY KEY,
    deal_id        UUID        NOT NULL REFERENCES deals(id) ON DELETE CASCADE,
    kind           TEXT        NOT NULL REFERENCES document_kinds(kind),
    status         TEXT        NOT NULL
                   CHECK (status IN ('missing', 'uploaded', 'under_review', 'approved', 'rejected')),
    reject_reason  TEXT,
    next_action_at TIMESTAMPTZ,
    updated_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (deal_id, kind)
);

CREATE INDEX deal_documents_next_action_idx ON deal_documents(next_action_at)
    WHERE next_action_at IS NOT NULL;

CREATE TABLE timeline_events (
    id           UUID PRIMARY KEY,
    deal_id      UUID        NOT NULL REFERENCES deals(id) ON DELETE CASCADE,
    seq          INT         NOT NULL,
    step         TEXT        NOT NULL,
    actor        TEXT        NOT NULL,
    status       TEXT        NOT NULL CHECK (status IN ('pending', 'in_progress', 'done', 'delayed')),
    delay_reason TEXT,
    started_at   TIMESTAMPTZ,
    finished_at  TIMESTAMPTZ,
    UNIQUE (deal_id, seq)
);

CREATE TABLE notifications (
    id         UUID PRIMARY KEY,
    company_id UUID        NOT NULL,
    deal_id    UUID        REFERENCES deals(id) ON DELETE CASCADE,
    severity   TEXT        NOT NULL CHECK (severity IN ('info', 'warning', 'critical')),
    message    TEXT        NOT NULL,
    read_at    TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX notifications_company_idx ON notifications(company_id, created_at DESC);

-- Transactional outbox: событие пишется в той же транзакции, что и изменение,
-- поэтому не теряется и не опережает данные. Порядок доставки — по id.
CREATE TABLE outbox (
    id           BIGINT      GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    company_id   UUID        NOT NULL,
    topic        TEXT        NOT NULL,
    payload      JSONB       NOT NULL,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    published_at TIMESTAMPTZ
);

CREATE INDEX outbox_unpublished_idx ON outbox(id) WHERE published_at IS NULL;
```

- [ ] **Step 3: Пробросить порт pg-core в `docker-compose.yml`**

В блок `pg-core` перед `volumes:` добавить:
```yaml
    ports:
      - "127.0.0.1:5435:5432"   # для api-тестов и ручной отладки
```

- [ ] **Step 4: Применить и проверить**

```bash
docker compose up -d pg-core migrate-core
docker compose logs migrate-core | tail -5
PGPASSWORD=core psql -h 127.0.0.1 -p 5435 -U core -d core \
  -c '\dt' -c 'SELECT kind, required_for FROM document_kinds ORDER BY kind;'
```
Expected: `APPLY: 002_deals.sql`, `Done. Applied ...`; шесть таблиц (`deals`, `deal_documents`, `document_kinds`, `notifications`, `outbox`, `timeline_events`, плюс `schema_migrations`), `_placeholder` отсутствует; пять строк справочника.

Проверить идемпотентность повторного применения:
```bash
docker compose run --rm migrate-core | tail -3
```
Expected: `SKIP: 002_deals.sql (already applied)`.

- [ ] **Step 5: Commit**

```bash
git add services/service-core/migrations docker-compose.yml
git commit -m "feat(core): deal domain schema and document kinds seed"
```

---

### Task 2: DealStateMachine — чистые переходы

**Files:**
- Create: `services/service-core/include/domain.h`, `services/service-core/src/domain.cc`, `services/service-core/include/state_machine.h`, `services/service-core/src/state_machine.cc`, `services/service-core/tests/CMakeLists.txt`, `services/service-core/tests/test_domain.cc`, `services/service-core/tests/test_state_machine.cc`
- Modify: `services/service-core/CMakeLists.txt`

**Interfaces:**
- Consumes: Catch2-каркас плана 02 (`BUILD_TESTS`, `Catch2::Catch2WithMain`, цель `common`).
- Produces (`core_svc`, заголовок `domain.h`):
  - `enum class Stage { created, documents, compliance_check, settlement, completed, blocked }` + `std::string_view toString(Stage)` + `std::optional<Stage> stageFromString(std::string_view)`
  - то же для `enum class DocStatus { missing, uploaded, under_review, approved, rejected }`, `enum class StepStatus { pending, in_progress, done, delayed }`, `enum class Scenario { cbdc, bank_transfer, smart_contract, trade_finance }`, `enum class OperationType { import_, export_ }`
  - `constexpr std::array<Scenario, 4> kScenarioOrder`
  - `struct DocumentState { std::string kind; DocStatus status; }`
  - `struct TimelineStep { int seq; std::string step; std::string actor; StepStatus status; std::string delay_reason; }`
  - `struct DealState { Stage stage; std::optional<Scenario> scenario; std::optional<Stage> blocked_from; std::vector<DocumentState> documents; std::vector<TimelineStep> timeline; }`
  - `std::string displayId(int year, long long display_no)`
  - `int progressPercent(const std::vector<TimelineStep> &timeline)`
- Produces (`state_machine.h`):
  - `struct TimelineChange { int seq; StepStatus status; std::string delay_reason; }`
  - `struct NotificationDraft { std::string severity; std::string message; }`
  - `struct Transition { Stage stage; std::optional<Stage> blocked_from; std::string blocker_reason; std::vector<TimelineChange> timeline; std::vector<NotificationDraft> notifications; bool schedule_next = false; }`
  - `struct TransitionError { std::string code; std::string message; }`
  - `using TransitionResult = std::variant<Transition, TransitionError>;`
  - `std::vector<TimelineStep> initialTimeline()` — семь шагов из спеки §4.2 со `step`/`actor`, первый `done`, остальные `pending`
  - `std::vector<std::string> requiredDocuments(Scenario)` — виды документов из `document_kinds.required_for`, зашитые в код (справочник в БД — для текстов, набор — для логики; расхождение ловит тест Task 6)
  - `std::string scenarioStepLabel(Scenario)` — подпись шестого шага таймлайна
  - `TransitionResult onScenarioConfirmed(const DealState &)`
  - `TransitionResult onDocumentSubmitted(const DealState &, const std::string &kind)`
  - `TransitionResult onDocumentReviewed(const DealState &, const std::string &kind, bool approved, const std::string &reject_reason)`
  - `TransitionResult onComplianceResult(const DealState &, bool approved, const std::string &reason)`
  - `TransitionResult onSettlementDone(const DealState &)`

- [ ] **Step 1: Написать падающие тесты `services/service-core/tests/test_domain.cc`**

```cpp
#include <catch2/catch_test_macros.hpp>

#include <domain.h>

using namespace core_svc;

TEST_CASE("строковые значения стадий совпадают с CHECK-ограничением в БД") {
  CHECK(toString(Stage::compliance_check) == "compliance_check");
  CHECK(stageFromString("settlement") == Stage::settlement);
  CHECK_FALSE(stageFromString("nonsense").has_value());
}

TEST_CASE("display_id собирается из года и номера") {
  CHECK(displayId(2026, 143) == "DEAL-2026-0143");
  CHECK(displayId(2026, 7) == "DEAL-2026-0007");
  CHECK(displayId(2026, 12345) == "DEAL-2026-12345");
}

TEST_CASE("прогресс считается по выполненным шагам таймлайна") {
  std::vector<TimelineStep> timeline{
      {1, "Сделка создана", "Вы", StepStatus::done, ""},
      {2, "Выбор сценария расчёта", "Вы", StepStatus::done, ""},
      {3, "Документы", "Вы", StepStatus::in_progress, ""},
      {4, "Комплаенс-проверка", "Банк", StepStatus::pending, ""},
  };

  CHECK(progressPercent(timeline) == 50);
  CHECK(progressPercent({}) == 0);
}

TEST_CASE("порядок сценариев фиксирован") {
  CHECK(kScenarioOrder[0] == Scenario::cbdc);
  CHECK(kScenarioOrder[3] == Scenario::trade_finance);
}
```

- [ ] **Step 2: Написать падающие тесты `services/service-core/tests/test_state_machine.cc`**

```cpp
#include <catch2/catch_test_macros.hpp>

#include <state_machine.h>

using namespace core_svc;

namespace {

DealState freshDeal() {
  DealState deal;
  deal.stage = Stage::created;
  deal.timeline = initialTimeline();
  return deal;
}

DealState dealInDocuments(Scenario scenario, DocStatus status) {
  DealState deal = freshDeal();
  deal.stage = Stage::documents;
  deal.scenario = scenario;
  for (const auto &kind : requiredDocuments(scenario))
    deal.documents.push_back({kind, status});
  deal.timeline[1].status = StepStatus::done;
  deal.timeline[2].status = StepStatus::in_progress;
  return deal;
}

const Transition &transition(const TransitionResult &result) {
  REQUIRE(std::holds_alternative<Transition>(result));
  return std::get<Transition>(result);
}

const TransitionError &error(const TransitionResult &result) {
  REQUIRE(std::holds_alternative<TransitionError>(result));
  return std::get<TransitionError>(result);
}

}  // namespace

TEST_CASE("изначальный таймлайн — семь шагов, первый выполнен") {
  const auto timeline = initialTimeline();
  REQUIRE(timeline.size() == 7);
  CHECK(timeline[0].status == StepStatus::done);
  CHECK(timeline[0].actor == "Вы");
  CHECK(timeline[6].step == "Завершение сделки");
  for (size_t i = 1; i < timeline.size(); ++i)
    CHECK(timeline[i].status == StepStatus::pending);
}

TEST_CASE("набор обязательных документов зависит от сценария") {
  CHECK(requiredDocuments(Scenario::cbdc) == std::vector<std::string>{"contract", "invoice"});
  CHECK(requiredDocuments(Scenario::bank_transfer) ==
        std::vector<std::string>{"contract", "invoice", "deal_passport"});
  CHECK(requiredDocuments(Scenario::trade_finance) ==
        std::vector<std::string>{"contract", "invoice", "deal_passport", "letter_of_credit"});
  CHECK(requiredDocuments(Scenario::smart_contract) ==
        std::vector<std::string>{"contract", "invoice", "digital_signature"});
}

TEST_CASE("подтверждение сценария переводит сделку к документам") {
  const auto result = transition(onScenarioConfirmed(freshDeal()));

  CHECK(result.stage == Stage::documents);
  CHECK(result.timeline.size() == 2);
  CHECK(result.timeline[0].seq == 2);
  CHECK(result.timeline[0].status == StepStatus::done);
  CHECK(result.timeline[1].seq == 3);
  CHECK(result.timeline[1].status == StepStatus::in_progress);
  CHECK(result.schedule_next == false);  // ждём документы от пользователя
}

TEST_CASE("сценарий нельзя подтвердить дважды после подачи документов") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::uploaded);

  CHECK(error(onScenarioConfirmed(deal)).code == "INVALID_TRANSITION");
}

TEST_CASE("подача документа ставит его в очередь на проверку") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::missing);

  const auto result = transition(onDocumentSubmitted(deal, "contract"));

  CHECK(result.stage == Stage::documents);
  CHECK(result.schedule_next == true);  // дальше работает эмулятор
}

TEST_CASE("нельзя подать документ, которого нет у сделки") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::missing);

  CHECK(error(onDocumentSubmitted(deal, "letter_of_credit")).code == "INVALID_TRANSITION");
}

TEST_CASE("нельзя переподать уже одобренный документ") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);

  CHECK(error(onDocumentSubmitted(deal, "contract")).code == "INVALID_TRANSITION");
}

TEST_CASE("одобрение последнего документа двигает сделку в комплаенс") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);
  deal.documents[1].status = DocStatus::under_review;

  const auto result = transition(onDocumentReviewed(deal, "invoice", true, ""));

  CHECK(result.stage == Stage::compliance_check);
  CHECK(result.schedule_next == true);
  REQUIRE(result.notifications.size() == 1);
  CHECK(result.notifications[0].severity == "info");
}

TEST_CASE("одобрение не последнего документа оставляет сделку на документах") {
  auto deal = dealInDocuments(Scenario::bank_transfer, DocStatus::missing);
  deal.documents[0].status = DocStatus::under_review;

  const auto result = transition(onDocumentReviewed(deal, "contract", true, ""));

  CHECK(result.stage == Stage::documents);
}

TEST_CASE("отклонение документа блокирует сделку с причиной") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::under_review);

  const auto result = transition(onDocumentReviewed(deal, "invoice", false, "Скан нечитаем"));

  CHECK(result.stage == Stage::blocked);
  CHECK(result.blocked_from == Stage::documents);
  CHECK(result.blocker_reason == "Скан нечитаем");
  REQUIRE(result.notifications.size() == 1);
  CHECK(result.notifications[0].severity == "critical");
}

TEST_CASE("переподача отклонённого документа возвращает сделку к документам") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);
  deal.stage = Stage::blocked;
  deal.blocked_from = Stage::documents;
  deal.documents[1].status = DocStatus::rejected;

  const auto result = transition(onDocumentSubmitted(deal, "invoice"));

  CHECK(result.stage == Stage::documents);
  CHECK_FALSE(result.blocked_from.has_value());
  CHECK(result.schedule_next == true);
}

TEST_CASE("успешный комплаенс переводит к расчёту") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);
  deal.stage = Stage::compliance_check;
  deal.timeline[2].status = StepStatus::done;
  deal.timeline[3].status = StepStatus::in_progress;

  const auto result = transition(onComplianceResult(deal, true, ""));

  CHECK(result.stage == Stage::settlement);
  CHECK(result.schedule_next == true);
}

TEST_CASE("отказ комплаенса блокирует сделку") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);
  deal.stage = Stage::compliance_check;

  const auto result = transition(onComplianceResult(deal, false, "ФНС не подтвердила контракт"));

  CHECK(result.stage == Stage::blocked);
  CHECK(result.blocked_from == Stage::compliance_check);
  CHECK(result.blocker_reason == "ФНС не подтвердила контракт");
}

TEST_CASE("завершение расчёта закрывает сделку") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);
  deal.stage = Stage::settlement;

  const auto result = transition(onSettlementDone(deal));

  CHECK(result.stage == Stage::completed);
  CHECK(result.schedule_next == false);
  REQUIRE(result.notifications.size() == 1);
}

TEST_CASE("нельзя завершить расчёт из стадии документов") {
  auto deal = dealInDocuments(Scenario::cbdc, DocStatus::approved);

  CHECK(error(onSettlementDone(deal)).code == "INVALID_TRANSITION");
}
```

`services/service-core/tests/CMakeLists.txt`:
```cmake
add_executable(core_tests
  test_domain.cc
  test_state_machine.cc
  ${CMAKE_CURRENT_SOURCE_DIR}/../src/domain.cc
  ${CMAKE_CURRENT_SOURCE_DIR}/../src/state_machine.cc
)

target_include_directories(core_tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../include)

target_link_libraries(core_tests PRIVATE
  Catch2::Catch2WithMain
)

catch_discover_tests(core_tests)
```
(домен и автомат не зависят ни от Drogon, ни от `common` — линкуем только Catch2.)

В `services/service-core/CMakeLists.txt` в конец добавить:
```cmake
if(BUILD_TESTS AND TARGET Catch2::Catch2WithMain)
  add_subdirectory(tests)
endif()
```

- [ ] **Step 3: Запустить — должно упасть**

Run: `make test-cpp 2>&1 | tail -20`
Expected: ошибка компиляции `domain.h: No such file or directory`.

- [ ] **Step 4: Реализовать `domain.h` / `domain.cc`**

Требования к реализации (код пишешь сам, интерфейс задан выше):
- Значения строк — ровно те, что в `CHECK`-ограничениях миграции Task 1.
- `displayId` — `"DEAL-" + year + "-" + zero-padded(display_no, min 4)`.
- `progressPercent` — `round(done * 100.0 / total)`, пустой таймлайн → `0`.
- `OperationType::import_`/`export_` сериализуются в `"import"`/`"export"` (подчёркивания — только чтобы не конфликтовать с ключевыми словами).
- Никаких include из Drogon: заголовок должен собираться в тестовой цели, которая линкует только Catch2.

- [ ] **Step 5: Реализовать `state_machine.h` / `state_machine.cc`**

Требования:
- `initialTimeline()` — семь шагов по спеке §4.2:
  1 «Сделка создана» / «Вы» / `done`; 2 «Выбор сценария расчёта» / «Вы»; 3 «Документы» / «Вы»;
  4 «Комплаенс-проверка» / «Банк»; 5 «Проверка ФНС» / «ФНС»;
  6 `scenarioStepLabel(scenario)` / актор по сценарию; 7 «Завершение сделки» / «Система».
  Шестой шаг до выбора сценария — «Расчёт» / «Банк»; после выбора репозиторий обновляет `step`/`actor` по `scenarioStepLabel`.
  `scenarioStepLabel`: `cbdc` → «Расчёт через ЦВЦБ» (актор «Платформа ЦВЦБ»), `bank_transfer` → «Банковский перевод» («Банк»), `smart_contract` → «Исполнение смарт-контракта» («Смарт-контракт»), `trade_finance` → «Раскрытие аккредитива» («Банк-гарант»).
- Переходы разрешены ровно те, что в тестах Step 2; всё остальное — `TransitionError{"INVALID_TRANSITION", "<по-русски, что именно нельзя>"}`.
- `onDocumentReviewed(approved=true)` двигает сделку в `compliance_check`, только когда все обязательные документы сделки одобрены (с учётом того, что статус проверяемого документа в `DealState` ещё старый).
- Уведомления: одобрение всех документов → `info` «Документы приняты, идёт комплаенс-проверка»; блокировка → `critical` с причиной; завершение → `info` «Сделка завершена».
- `schedule_next` — признак «дальше работает эмулятор»: true после подачи документа, после одобрения последнего документа, после успешного комплаенса; false, когда ждём действия пользователя или сделка завершена.
- Файл не должен превышать ~250 строк; если растёт — вынести таблицу шагов в `domain.cc`.

- [ ] **Step 6: Прогнать тесты**

Run: `make test-cpp`
Expected: 17 прежних + 20 новых = 37/37 PASS (точное число может отличаться — важно, что всё зелёное).

- [ ] **Step 7: Commit**

```bash
git add services/service-core
git commit -m "feat(core): deal domain types and state machine with Catch2 specs"
```

---

### Task 3: Репозиторий и чтение сделок

**Files:**
- Create: `services/service-core/include/repository.h`, `services/service-core/src/repository.cc`, `services/service-core/src/core/deals.cc`
- Modify: `services/service-core/include/core_controller.h`, `services/service-core/src/core/health.cc`, `services/service-core/config.json.tpl`, `docker-compose.yml`
- Delete: `services/service-core/src/core/compute.cc`, `services/service-core/src/core/status.cc`

**Interfaces:**
- Consumes: домен и автомат (Task 2), схема (Task 1), `common::JwtFilter`/`common::jsonError` (план 02).
- Produces (`core_svc`, `repository.h`):
  - `struct DealRow { std::string id, company_id, counterparty_country, counterparty_name, currency; long long display_no; int year; OperationType operation_type; std::string amount; std::optional<Scenario> scenario; std::optional<std::string> commission_total; Json::Value commission_breakdown; Stage stage; std::optional<Stage> blocked_from; std::string blocker_reason; int version; std::string created_at, updated_at; }`
  - `struct DealDetail { DealRow deal; std::vector<DocumentRow> documents; std::vector<TimelineStep> timeline; }`, где `struct DocumentRow { std::string id, kind, title, purpose; DocStatus status; std::string reject_reason, updated_at; }`
  - `class DealRepository` со статическими корутинами:
    `static drogon::Task<std::optional<DealDetail>> find(std::string deal_id, std::string company_id);`
    `static drogon::Task<std::vector<DealRow>> list(std::string company_id, int limit);`
    (в Task 4 к ним добавятся корутины записи)
  - `DealState toDealState(const DealDetail &)` — мост в автомат
  - `Json::Value dealSummaryJson(const DealRow &)`, `Json::Value dealJson(const DealDetail &)` — сериализация для API (в summary входит `progress_percent`, `display_id`, `needs_attention`)
- Produces (HTTP): `GET /api/core/deals?limit=`, `GET /api/core/deals/{id}`; `/health` с `redis_ok`. Ручки `compute`/`status` исчезают.

- [ ] **Step 1: Убрать заглушки и объявить новые ручки**

Удалить `services/service-core/src/core/compute.cc` и `src/core/status.cc` (`git rm`).
В `services/service-core/include/core_controller.h` заменить блоки `compute`/`status` (и их объявления) на:
```cpp
  // @GET /api/core/deals
  // @summary Список сделок компании
  // @header Authorization: Bearer <token>
  // @200    {"items": "array", "count": "integer"}
  // @401    {"code": "string", "error": "string"}
  ADD_METHOD_TO(CoreController::listDeals, "/api/core/deals?limit={limit}", drogon::Get,
                "common::JwtFilter");

  // @GET /api/core/deals/{id}
  // @summary Сделка целиком: документы и таймлайн
  // @header Authorization: Bearer <token>
  // @200    {"deal": "object"}
  // @404    {"code": "string", "error": "string"}
  ADD_METHOD_TO(CoreController::getDeal, "/api/core/deals/{id}", drogon::Get,
                "common::JwtFilter");
```
и объявления:
```cpp
  static drogon::Task<> listDeals(drogon::HttpRequestPtr req,
                                  std::function<void(const drogon::HttpResponsePtr &)> cb,
                                  std::string limit);
  static drogon::Task<> getDeal(drogon::HttpRequestPtr req,
                                std::function<void(const drogon::HttpResponsePtr &)> cb,
                                std::string id);
```

- [ ] **Step 2: Подключить Redis к конфигу сервиса**

В `services/service-core/config.json.tpl` после массива `db_clients` добавить:
```json
  ,
  "redis_clients": [
    {
      "name": "default",
      "host": "${REDIS_HOST}",
      "port": ${REDIS_PORT},
      "db": 0,
      "is_fast": false,
      "number_of_connections": 2
    }
  ]
```
В `docker-compose.yml` в `environment` сервиса `service-core` добавить:
```yaml
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      COMMISSION_URL: "http://service-commission:8000"
      EMULATOR_ENABLED: "true"
      EMULATOR_SPEED: demo
```

- [ ] **Step 3: Реализовать `repository.h` / `repository.cc` (чтение)**

Требования:
- `find` одним запросом достаёт сделку с проверкой `company_id` (`WHERE id = $1 AND company_id = $2`), затем документы (JOIN `document_kinds` ради `title`/`purpose`, `ORDER BY document_kinds.kind`) и таймлайн (`ORDER BY seq`). Три запроса — нормально, N+1 здесь нет.
- `list` — `SELECT ... WHERE company_id = $1 ORDER BY updated_at DESC LIMIT $2`, `limit` зажимается в диапазон 1..100 (по умолчанию 50).
- `amount` и `commission_total` читаются из БД **дважды разными способами и это намеренно**: как строка (`as<std::string>()`) — её `DealRow` хранит и передаёт в service-commission без потери точности NUMERIC; как `double` — только для сериализации в JSON, потому что контракт API требует число. Пояснить это комментарием в коде, чтобы следующий не «упростил».
- `year` для `display_id` берётся из `EXTRACT(YEAR FROM created_at)` в SQL — не из локального времени процесса.
- `dealSummaryJson` отдаёт: `id`, `display_id`, `counterparty_name`, `counterparty_country`, `operation_type`, `amount`, `currency`, `scenario` (или `null`), `stage`, `progress_percent`, `needs_attention` (= `stage == blocked`), `attention_reason` (или `null`), `commission_total` (или `null`), `version`, `updated_at`.
- `dealJson` = summary + `documents` (`id`, `kind`, `title`, `purpose`, `status`, `reject_reason`) + `timeline` (`seq`, `step`, `actor`, `status`, `delay_reason`, `started_at`, `finished_at`) + `created_at`.
- Все времена — ISO-8601 UTC строкой (`to_char(... , 'YYYY-MM-DD"T"HH24:MI:SS"Z"')` в SQL, чтобы не заводить форматирование в C++).

- [ ] **Step 4: Реализовать `src/core/deals.cc` (чтение)**

`listDeals`: `company_id` из атрибутов, `limit` из query (пустая строка → 50, нечисловое → `400 VALIDATION_ERROR`), ответ `{"items": [...], "count": N}`.
`getDeal`: `find(id, company_id)`; пусто → `404 NOT_FOUND` «Сделка не найдена»; иначе `{"deal": {...}}`.
Оба обработчика ловят `orm::DrogonDbException` и `std::exception` и отвечают `500 INTERNAL_ERROR` (как в auth).

- [ ] **Step 5: Добавить `redis_ok` в `/health`**

В `services/service-core/src/core/health.cc` после проверки Postgres добавить проверку Redis:
```cpp
  try {
    auto redis = app().getRedisClient();
    co_await redis->execCommandCoro("ping");
    j["redis_ok"] = true;
  } catch (...) {
    j["redis_ok"] = false;
    j["ok"] = false;
  }
```

- [ ] **Step 6: Проверить сборку и стенд**

```bash
make test-cpp
docker compose up -d --build pg-core migrate-core redis service-core
docker compose ps service-core
docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s http://service-core:8080/health
```
Expected: тесты зелёные; health отдаёт `{"ok":true, ..., "postgres_ok":true, "redis_ok":true}`.
Затем проверить изоляцию и 401 (токены берём у service-auth; если он не поднят — `docker compose up -d --build service-auth pg-auth migrate-auth`):
```bash
docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s -o /dev/null -w '%{http_code}\n' \
  http://service-core:8080/api/core/deals
TOKEN=$(docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s -X POST \
  http://service-auth:8080/api/auth/login -H 'Content-Type: application/json' \
  -d '{"login":"egor","password":"hunter22"}' | sed 's/.*"token":"//; s/".*//')
docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s \
  http://service-core:8080/api/core/deals -H "Authorization: Bearer $TOKEN"
```
Expected: без токена — `401`; с токеном — `{"items":[],"count":0}`.
(Если логина `egor` нет — зарегистрировать его так же, как в плане 02.)

- [ ] **Step 7: Commit**

```bash
git add services/service-core docker-compose.yml
git commit -m "feat(core): deal repository and read endpoints, drop compute/status stubs"
```

---

### Task 4: Создание сделки, выбор сценария, подача документов

**Files:**
- Create: `services/service-core/include/commission_client.h`, `services/service-core/src/commission_client.cc`, `services/service-core/src/core/scenario.cc`, `services/service-core/src/core/documents.cc`
- Modify: `services/service-core/include/core_controller.h`, `services/service-core/include/repository.h`, `services/service-core/src/repository.cc`, `services/service-core/src/core/deals.cc`

**Interfaces:**
- Consumes: автомат (Task 2), репозиторий-чтение (Task 3), API `POST /api/commission/quotes` сервиса service-commission (план 01).
- Produces (`commission_client.h`):
  - `struct Quote { Scenario scenario; bool available; std::string unavailable_reason; double total = 0; Json::Value breakdown; }`
  - `struct CommissionError { int status; std::string code, message; }`
  - `using QuotesResult = std::variant<std::vector<Quote>, CommissionError>;`
  - `drogon::Task<QuotesResult> fetchQuotes(std::string authorization, std::string from_country, std::string to_country, std::string currency, std::string amount);`
- Produces (`repository.h`): корутины записи
  - `static drogon::Task<DealDetail> create(...)` — сделка + семь шагов таймлайна + outbox, одной транзакцией
  - `static drogon::Task<ApplyResult> apply(std::string deal_id, std::string company_id, int expected_version, const Transition &transition, const DealMutation &mutation)` — общий применятель перехода: обновляет сделку, документы, таймлайн, пишет уведомления и outbox в одной транзакции; `struct ApplyResult { enum class Status { ok, not_found, version_conflict } status; std::optional<DealDetail> deal; }`
  - `struct DealMutation { std::optional<Scenario> scenario; std::optional<std::string> commission_total; Json::Value commission_breakdown; std::optional<std::string> document_kind; std::optional<DocStatus> document_status; std::string document_reject_reason; std::optional<long long> next_action_in_sec; }`
- Produces (HTTP): `POST /api/core/deals`, `POST /api/core/deals/{id}/scenario`, `POST /api/core/deals/{id}/documents/{docId}/submit`.

- [ ] **Step 1: Объявить ручки в `core_controller.h`**

```cpp
  // @POST /api/core/deals
  // @summary Создание сделки
  // @header Authorization: Bearer <token>
  // @body   {"counterparty_country": "string", "counterparty_name": "string", "operation_type": "string", "amount": "number", "currency": "string"}
  // @201    {"deal": "object"}
  // @400    {"code": "string", "error": "string"}
  ADD_METHOD_TO(CoreController::createDeal, "/api/core/deals", drogon::Post, "common::JwtFilter");

  // @POST /api/core/deals/{id}/scenario
  // @summary Подтверждение сценария расчёта (комиссия пересчитывается на сервере)
  // @header Authorization: Bearer <token>
  // @body   {"scenario": "string", "version": "integer"}
  // @200    {"deal": "object"}
  // @409    {"code": "string", "error": "string"}
  ADD_METHOD_TO(CoreController::chooseScenario, "/api/core/deals/{id}/scenario", drogon::Post,
                "common::JwtFilter");

  // @POST /api/core/deals/{dealId}/documents/{docId}/submit
  // @summary Подача документа на проверку
  // @header Authorization: Bearer <token>
  // @body   {"version": "integer"}
  // @200    {"deal": "object"}
  // @409    {"code": "string", "error": "string"}
  ADD_METHOD_TO(CoreController::submitDocument,
                "/api/core/deals/{dealId}/documents/{docId}/submit", drogon::Post,
                "common::JwtFilter");
```
плюс объявления методов с соответствующими path-параметрами.

- [ ] **Step 2: Реализовать `commission_client`**

Требования:
- Базовый URL из env `COMMISSION_URL` (по умолчанию `http://service-commission:8000`), клиент создаётся один раз (`static`), таймаут 5 секунд.
- `fetchQuotes` шлёт `POST /api/commission/quotes` с телом `{from_country, to_country, currency, amount}` (amount — число из строки) и заголовком `Authorization` пользователя (проброс as-is).
- Разбор ответа 200: массив `quotes`, каждый элемент → `Quote` (`commission.total` → `total`, весь объект `commission` → `breakdown`).
- Маппинг ошибок:
  - 401 → `CommissionError{401, "<код из ответа или INVALID_TOKEN>", "<сообщение>"}`
  - 400/404 → `CommissionError{400, "<код из ответа>", "<сообщение>"}`
  - 5xx, таймаут, отсутствие ответа → `CommissionError{503, "UPSTREAM_UNAVAILABLE", "Сервис расчёта комиссии недоступен"}`
- Никаких повторов: BFF и пользователь увидят 503 и повторят сами.

- [ ] **Step 3: Реализовать `create` в репозитории и `createDeal`**

- Валидация тела: `counterparty_country` — 2 буквы (приводится к верхнему регистру), `counterparty_name` — непустой после trim, `operation_type` ∈ {import, export}, `amount` > 0 и не больше двух знаков после запятой, `currency` — 3 буквы (верхний регистр). Ошибка → `400 VALIDATION_ERROR` с конкретным сообщением по-русски.
- Транзакция: INSERT сделки (`stage = 'created'`, `version = 0`), INSERT семи шагов `initialTimeline()`, INSERT в `outbox` (`topic = 'deal-events'`, payload `{"type":"deal.created","deal_id":...}`).
- Ответ `201 {"deal": {...}}` — полный `dealJson` (документов ещё нет, они появятся при выборе сценария).

- [ ] **Step 4: Реализовать `apply` в репозитории**

Одна транзакция:
1. `SELECT ... FROM deals WHERE id = $1 AND company_id = $2 FOR UPDATE` → пусто → `not_found`.
2. Если `version != expected_version` → `version_conflict`.
3. `UPDATE deals SET stage, blocked_from, blocker_reason, scenario?, commission_*?, next_action_at?, version = version + 1, updated_at = now() WHERE id = $1`.
4. Документы: при `mutation.document_kind` — `UPDATE deal_documents SET status, reject_reason, next_action_at, updated_at = now() WHERE deal_id = $1 AND kind = $2`; при подтверждении сценария — INSERT недостающих документов из `requiredDocuments(scenario)` со статусом `missing` (`ON CONFLICT (deal_id, kind) DO NOTHING`).
5. Таймлайн: для каждого `TimelineChange` — `UPDATE timeline_events SET status, delay_reason, started_at = COALESCE(started_at, now()), finished_at = CASE WHEN $status = 'done' THEN now() ELSE finished_at END WHERE deal_id = $1 AND seq = $2`. При подтверждении сценария дополнительно обновляется `step`/`actor` шестого шага по `scenarioStepLabel`.
6. Уведомления: INSERT каждого `NotificationDraft`; на каждое — запись в outbox с `{"type":"notification.created","notification_id":...,"deal_id":...}`.
7. Одна запись в outbox `{"type":"deal.updated","deal_id":...}`.
8. Возврат свежего `DealDetail` (перечитать в той же транзакции).

- [ ] **Step 5: Реализовать `chooseScenario`**

- Тело: `scenario` (из четырёх) и `version` (целое ≥ 0). Нет/не то → `400 VALIDATION_ERROR`.
- `find` → нет → `404 NOT_FOUND`.
- Проверить переход `onScenarioConfirmed` **до** похода в commission (не дёргаем чужой сервис зря); ошибка → `409 INVALID_TRANSITION`.
- Направление коридора: `import` → `from = "RU"`, `to = counterparty_country`; `export` → наоборот.
- `fetchQuotes` с `Authorization` исходного запроса. Ошибка → соответствующий статус и код (`401`, `400`, `503 UPSTREAM_UNAVAILABLE`).
- Найти котировку выбранного сценария: нет в ответе или `available == false` → `409 SCENARIO_UNAVAILABLE` с `unavailable_reason` в сообщении.
- `apply(...)` с `DealMutation{scenario, commission_total, commission_breakdown}` и переходом. `version_conflict` → `409 VERSION_CONFLICT` «Сделка изменилась, обновите страницу»; `not_found` → `404`.
- Ответ `{"deal": {...}}`.

- [ ] **Step 6: Реализовать `submitDocument`**

- Путь содержит `docId` — это **id документа**, а не `kind`: найти документ в `DealDetail` по id; нет → `404 NOT_FOUND`.
- `version` из тела обязателен.
- `onDocumentSubmitted(state, kind)`; ошибка → `409 INVALID_TRANSITION` с сообщением автомата.
- `apply` с `DealMutation{document_kind, document_status = uploaded, next_action_in_sec = <env EMULATOR_DOC_DELAY_SEC, по умолчанию 5>}`. В Task 5 это значение начнёт браться из `EmulatorConfig::doc_review_sec`; здесь достаточно чтения env в одном месте.
- Ответ `{"deal": {...}}`.

- [ ] **Step 7: Проверить на стенде**

```bash
make test-cpp
docker compose up -d --build service-core service-commission
TOKEN=...   # логин, как в Task 3
CURL="docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s"
$CURL -X POST http://service-core:8080/api/core/deals -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"counterparty_country":"CN","counterparty_name":"Shenzhen Bay Trading Co.","operation_type":"import","amount":12400000,"currency":"RUB"}'
# из ответа взять id и version
$CURL -X POST http://service-core:8080/api/core/deals/<id>/scenario -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' -d '{"scenario":"cbdc","version":0}'
$CURL -X POST http://service-core:8080/api/core/deals/<id>/scenario -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' -d '{"scenario":"cbdc","version":0}'
```
Expected: сделка создаётся (`stage: "created"`, `progress_percent` ≈ 14); выбор сценария возвращает `stage: "documents"`, ненулевой `commission_total`, два документа `contract`/`invoice` в статусе `missing`; **повторный** запрос с той же `version` → `409 VERSION_CONFLICT`.
Затем подать документ и убедиться, что его статус стал `uploaded`.

- [ ] **Step 8: Commit**

```bash
git add services/service-core
git commit -m "feat(core): create deal, confirm scenario with authoritative commission, submit documents"
```

---

### Task 5: Outbox → Redis и эмулятор внешних систем

**Files:**
- Create: `services/service-core/include/outbox_publisher.h`, `services/service-core/src/outbox_publisher.cc`, `services/service-core/include/emulator.h`, `services/service-core/src/emulator.cc`, `services/service-core/src/core/emulator_tick.cc`, `services/service-core/tests/test_emulator.cc`
- Modify: `services/service-core/src/main.cc`, `services/service-core/include/core_controller.h`, `services/service-core/tests/CMakeLists.txt`, `docker-compose.yml`

**Interfaces:**
- Consumes: `apply`/`find` репозитория (Task 4), автомат (Task 2), таблица `outbox` (Task 1), Redis-клиент Drogon (`app().getRedisClient()`).
- Produces (`emulator.h`):
  - `unsigned long long fnv1a(std::string_view)` — стабильный хэш (одинаковый на любой машине и в любом запуске)
  - `struct EmulatorConfig { long long doc_review_sec; long long compliance_sec; long long settlement_sec; }` + `EmulatorConfig emulatorConfigFromEnv()` (`EMULATOR_SPEED=demo` → 5/8/6 секунд, `realistic` → 120/300/180)
  - `bool documentApproved(std::string_view deal_id, std::string_view kind)` — `fnv1a(deal_id + ":" + kind) % 10 != 0` (10% отказов)
  - `std::string documentRejectReason(std::string_view deal_id, std::string_view kind)` — одна из двух причин, выбор по хэшу
  - `bool complianceDelayed(std::string_view deal_id)` — `fnv1a(deal_id + ":compliance") % 5 == 0` (20% задержек)
  - `class Emulator { static void start(const EmulatorConfig &); static drogon::Task<int> tickOnce(); }` — `tickOnce` возвращает число обработанных сущностей
- Produces (`outbox_publisher.h`): `class OutboxPublisher { static void start(); static drogon::Task<int> publishOnce(); }`
- Produces (HTTP): `POST /internal/emulator/tick` — регистрируется только при `EMULATOR_MANUAL=true`.

- [ ] **Step 1: Написать падающие тесты `services/service-core/tests/test_emulator.cc`**

```cpp
#include <catch2/catch_test_macros.hpp>

#include <emulator.h>

#include <cstdlib>
#include <set>
#include <string>

using namespace core_svc;

TEST_CASE("хэш стабилен и не зависит от прогона") {
  CHECK(fnv1a("deal-1") == fnv1a("deal-1"));
  CHECK(fnv1a("deal-1") != fnv1a("deal-2"));
}

TEST_CASE("исход проверки документа детерминирован по сделке и виду документа") {
  const bool first = documentApproved("11111111-1111-4111-8111-111111111111", "contract");
  for (int i = 0; i < 5; ++i)
    CHECK(documentApproved("11111111-1111-4111-8111-111111111111", "contract") == first);
}

TEST_CASE("отказы редки, но случаются") {
  int rejected = 0;
  for (int i = 0; i < 200; ++i)
    if (!documentApproved("deal-" + std::to_string(i), "contract"))
      ++rejected;

  CHECK(rejected > 0);
  CHECK(rejected < 60);  // ожидаем около 10%, ловим только грубые поломки
}

TEST_CASE("причина отказа — осмысленный текст из фиксированного набора") {
  const std::set<std::string> known{"Скан нечитаем", "Сумма не совпадает с инвойсом"};

  for (int i = 0; i < 50; ++i) {
    const auto reason = documentRejectReason("deal-" + std::to_string(i), "contract");
    CHECK(known.count(reason) == 1);
  }
}

TEST_CASE("задержки комплаенса детерминированы и нечасты") {
  const bool first = complianceDelayed("deal-7");
  CHECK(complianceDelayed("deal-7") == first);

  int delayed = 0;
  for (int i = 0; i < 200; ++i)
    if (complianceDelayed("deal-" + std::to_string(i)))
      ++delayed;

  CHECK(delayed > 0);
  CHECK(delayed < 100);
}

TEST_CASE("скорость эмулятора берётся из окружения") {
  setenv("EMULATOR_SPEED", "demo", 1);
  const auto demo = emulatorConfigFromEnv();
  CHECK(demo.doc_review_sec < 60);

  setenv("EMULATOR_SPEED", "realistic", 1);
  const auto realistic = emulatorConfigFromEnv();
  CHECK(realistic.doc_review_sec >= 60);

  unsetenv("EMULATOR_SPEED");
}
```

В `services/service-core/tests/CMakeLists.txt` добавить `test_emulator.cc` и `${CMAKE_CURRENT_SOURCE_DIR}/../src/emulator.cc` в `add_executable`, а в `target_link_libraries` — `common` и `Drogon::Drogon` (эмулятор тянет корутины Drogon).
Чистые функции (`fnv1a`, `documentApproved`, `documentRejectReason`, `complianceDelayed`, `emulatorConfigFromEnv`) обязаны собираться без запущенного фреймворка — не обращаться к `app()` на уровне файла.

- [ ] **Step 2: Запустить — должно упасть**

Run: `make test-cpp 2>&1 | tail -10`
Expected: `emulator.h: No such file or directory`.

- [ ] **Step 3: Реализовать эмулятор**

Требования:
- `fnv1a` — 64-битный FNV-1a по байтам строки (не `std::hash`: тот не гарантирует стабильность между запусками и платформами).
- `Emulator::tickOnce()` за один проход:
  1. Документы: `SELECT d.id, d.deal_id, d.kind, d.status, dl.company_id FROM deal_documents d JOIN deals dl ON dl.id = d.deal_id WHERE d.next_action_at IS NOT NULL AND d.next_action_at <= now() FOR UPDATE OF d SKIP LOCKED LIMIT 50`.
     - `uploaded` → перевести в `under_review`, `next_action_at = now() + doc_review_sec` (это отдельный шаг, чтобы пользователь увидел «на проверке»);
     - `under_review` → по `documentApproved` одобрить или отклонить, `next_action_at = NULL`, и применить переход `onDocumentReviewed` через `DealRepository::apply` (версия берётся текущая — эмулятор не участвует в optimistic locking пользователя, но обязан читать её в той же транзакции).
  2. Сделки: `SELECT id, company_id, stage FROM deals WHERE next_action_at IS NOT NULL AND next_action_at <= now() AND stage <> 'completed' FOR UPDATE SKIP LOCKED LIMIT 50`.
     - `compliance_check`: если `complianceDelayed(deal_id)` и шаг «Проверка ФНС» ещё не был `delayed` — пометить шаг `delayed` с текстом «Ожидаем ответ от ФНС», продлить `next_action_at`; иначе — `onComplianceResult(approved = true)`.
     - `settlement`: `onSettlementDone`.
  3. Вернуть количество обработанных записей.
- `Emulator::start(cfg)` — `app().getLoop()->runEvery(EMULATOR_TICK_SEC (по умолчанию 2.0), ...)`, внутри `drogon::async_run([]() -> Task<> { co_await tickOnce(); })`; исключения логировать и глотать, иначе таймер умрёт.
- Ошибки БД внутри тика логируются и не валят сервис.

- [ ] **Step 4: Реализовать publisher**

- `publishOnce()`: транзакция → `SELECT id, company_id, payload FROM outbox WHERE published_at IS NULL ORDER BY id FOR UPDATE SKIP LOCKED LIMIT 100`; для каждой строки `PUBLISH deal-events:<company_id> <payload с полем seq = id>`; затем `UPDATE outbox SET published_at = now() WHERE id = ANY($1)`.
- Публикация в Redis идёт **после** чтения, но `published_at` проставляется в той же транзакции: at-least-once (при падении между PUBLISH и COMMIT событие уедет повторно — потребитель идемпотентен по `seq`).
- `start()` — `runEvery(0.5, ...)`, ошибки логируются и глотаются.
- В `main.cc` после `app().run()`… — нет: регистрировать через `app().registerBeginningAdvice([]{ OutboxPublisher::start(); if (emulatorEnabled()) Emulator::start(emulatorConfigFromEnv()); });` **до** `app().run()`, иначе клиенты БД/Redis ещё не готовы.

- [ ] **Step 5: Ручка ручного тика**

В `core_controller.h` объявить `tickEmulator`; регистрировать маршрут `POST /internal/emulator/tick` **только** при `EMULATOR_MANUAL=true` (проверка в `main.cc` через `app().registerHandler(...)`, а не `ADD_METHOD_TO`, чтобы в обычном режиме ручки не существовало вовсе). Ответ `{"processed": N}`. Без JWT (внутренняя ручка, наружу не публикуется — в Traefik её нет).
При `EMULATOR_MANUAL=true` фоновый цикл эмулятора не запускается.

- [ ] **Step 6: Проверить сквозной сценарий на стенде**

```bash
make test-cpp
docker compose up -d --build service-core
CURL="docker run --rm --network ivr_backend-net curlimages/curl:8.10.1 -s"
# создать сделку, выбрать сценарий, подать оба документа (как в Task 4)
# затем наблюдать автоматическое продвижение:
for i in $(seq 1 12); do
  $CURL http://service-core:8080/api/core/deals/<id> -H "Authorization: Bearer $TOKEN" \
    | sed 's/.*"stage":"\([a-z_]*\)".*/\1/'
  sleep 5
done
docker compose logs --tail 30 service-core
```
Expected: стадия проходит `documents → compliance_check → settlement → completed`; в логах нет исключений.
Проверить события Redis (в отдельном окне до подачи документов):
```bash
docker compose exec redis redis-cli --timeout 60 psubscribe 'deal-events:*'
```
Expected: приходят сообщения с `seq`, `type` (`deal.updated`, `notification.created`) и `deal_id`; `seq` уникален для дедупликации повторных доставок — не гарантия порядка (см. §4.4 дизайн-документа и комментарий над `SELECT` в `outbox_publisher.cc`).

- [ ] **Step 7: Commit**

```bash
git add services/service-core docker-compose.yml
git commit -m "feat(core): transactional outbox publisher and deterministic external-systems emulator"
```

---

### Task 6: Уведомления, API-тесты, документация

**Files:**
- Create: `services/service-core/src/core/notifications.cc`, `tests/api/conftest.py`, `tests/api/test_core_deals.py`, `tests/api/pyproject.toml`
- Modify: `services/service-core/include/core_controller.h`, `Makefile`, `README.md`, `docs/openapi.yml`, `docs/superpowers/plans/2026-09-15-00-roadmap.md`

**Interfaces:**
- Consumes: весь core (Tasks 1–5), service-auth (`/api/auth/register`, `/api/auth/login`), service-commission.
- Produces (HTTP): `GET /api/core/notifications?limit=`, `POST /api/core/notifications/{id}/read`.
- Produces: `make test-api` — pytest против поднятого compose-стенда; `make smoke-deal` — сквозной сценарий одной командой.

- [ ] **Step 1: Ручки уведомлений**

- `GET /api/core/notifications?limit=` → `{"items": [{"id","deal_id","severity","message","read","created_at"}], "unread": N}`; фильтр по `company_id`, сортировка `created_at DESC`, лимит 1..100 (по умолчанию 50).
- `POST /api/core/notifications/{id}/read` → `UPDATE notifications SET read_at = now() WHERE id = $1 AND company_id = $2 AND read_at IS NULL`; ноль строк и отсутствие записи → `404 NOT_FOUND`; успех → `204` без тела.
- Аннотации для OpenAPI — как у остальных ручек.

- [ ] **Step 2: Написать API-тесты `tests/api/test_core_deals.py`**

`tests/api/pyproject.toml`:
```toml
[project]
name = "ivr-api-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28", "pytest>=8.4"]

[tool.pytest.ini_options]
testpaths = ["."]
```

`tests/api/conftest.py`:
- фикстуры `auth_url` и `core_url` — из env `AUTH_URL`/`CORE_URL`, по умолчанию `http://127.0.0.1:18080` и `http://127.0.0.1:18081` (эти порты пробрасывает compose, см. Step 3);
- сессионная фикстура `stack_ready` — опрашивает `/health` обоих сервисов с таймаутом 60 секунд и падает с понятным сообщением, если стенд не поднялся;
- фикстура `company` — регистрирует уникального пользователя (логин и ИНН с `uuid4`/случайными 10 цифрами) и возвращает `{"token": ..., "company_id": ...}`;
- фикстура `other_company` — то же для второй компании (нужна для проверки изоляции).

Тесты (каждый — отдельная проверка контракта, а не «пройдёмся по всему»):
- создание сделки возвращает `201`, `stage == "created"`, `version == 0`, непустой `display_id` вида `DEAL-<год>-<номер>`;
- список сделок компании содержит созданную, а список **другой** компании — нет;
- `GET /api/core/deals/<чужой id>` → `404` с `code == "NOT_FOUND"`;
- выбор сценария: `commission_total > 0`, `stage == "documents"`, документы `contract`/`invoice` в статусе `missing`;
- повтор с той же `version` → `409 VERSION_CONFLICT`;
- недоступный сценарий (`trade_finance` на маленькой сумме) → `409 SCENARIO_UNAVAILABLE`;
- подача документа → `uploaded`; ручной тик эмулятора (`POST /internal/emulator/tick`, стенд поднимается с `EMULATOR_MANUAL=true`) доводит сделку до `completed`, а в `/api/core/notifications` появляются записи;
- запрос без токена → `401 UNAUTHORIZED`; с мусорным токеном → `401 INVALID_TOKEN`.

- [ ] **Step 3: Compose-профиль для API-тестов**

В `docker-compose.yml` добавить профиль, который пробрасывает порты и включает ручной режим эмулятора:
```yaml
  service-auth:
    # ... существующее ...
    ports:
      - "127.0.0.1:18080:8080"

  service-core:
    # ... существующее ...
    ports:
      - "127.0.0.1:18081:8080"
```
и в `environment` core — `EMULATOR_MANUAL: "${EMULATOR_MANUAL:-false}"`, чтобы `EMULATOR_MANUAL=true make test-api` поднимал стенд в детерминированном режиме.

- [ ] **Step 4: Цели `Makefile`**

```makefile
test-api:
	EMULATOR_MANUAL=true docker compose up -d --build \
		pg-auth migrate-auth service-auth \
		pg-core migrate-core service-core \
		pg-commission migrate-commission service-commission redis
	cd tests/api && uv run --with httpx --with pytest pytest -q

smoke-deal:
	@if [ -z "$$TOKEN" ]; then echo "set TOKEN=..."; exit 1; fi
	bash infra/smoke-deal.sh
```
`infra/smoke-deal.sh` — сквозной сценарий из Task 5 Step 6 одной командой (создать сделку → сценарий → документы → тики эмулятора → напечатать финальную стадию и уведомления). Скрипт должен быть идемпотентным и печатать понятный вывод.

- [ ] **Step 5: Прогнать API-тесты**

Run: `make test-api`
Expected: все тесты зелёные. Если стенд не успевает подняться — добавить в `conftest.py` ожидание готовности (`/health` обоих сервисов, таймаут 60 секунд), а не `sleep` в Makefile.

- [ ] **Step 6: Документация**

- `make openapi` — перегенерировать `docs/openapi.yml`; убедиться, что `/api/core/compute` и `/api/core/status` исчезли, а ручки домена появились.
- `README.md`: в ASCII-схеме заменить описание service-core на «домен сделок»; в разделе «Как добавлять новые ручки» обновить пример для core; добавить раздел «Домен сделки» с жизненным циклом (схема стадий из спеки §4.2), описанием эмулятора (`EMULATOR_SPEED`, `EMULATOR_MANUAL`) и событий (`deal-events:{company_id}`, `seq`); в быстрый старт добавить `make test-api` и `make smoke-deal`; из «Что НЕ сделано» убрать пункты, которые этот план закрыл.
- `docs/superpowers/plans/2026-09-15-00-roadmap.md`: отметить план 03 выполненным; перенести в «Перенесено из плана 03» всё, что осталось (в частности: `tests/api` заведён здесь, но в CI его включает план 04).

- [ ] **Step 7: Финальная проверка**

Run: `make test-cpp && make test-api && git status --short`
Expected: C++ и API-тесты зелёные, в дереве только файлы предстоящего коммита.

- [ ] **Step 8: Commit**

```bash
git add services/service-core tests/api Makefile infra/smoke-deal.sh README.md docs
git commit -m "feat(core): notifications endpoints, API tests against the stack, docs"
```
